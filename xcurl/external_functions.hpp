#ifndef _EXTERNAL_FUNCTIONS_HPP
#define _EXTERNAL_FUNCTIONS_HPP

#include<vector>
#include<string>

std::string demultiply(std::string str, char x);
	std::vector<std::string> split(const std::string& str, const std::string& delim);
	std::string charXchange(std::string&, char, char);
	

#endif
